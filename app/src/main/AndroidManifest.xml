<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ttcntt.android.onthivnpt">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher01"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.ttcntt.android.onthivnpt.TagListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.ttcntt.android.onthivnpt.QuestionActivity"></activity>
    </application>

</manifest>